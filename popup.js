document.addEventListener('DOMContentLoaded', () => {
  const output = document.getElementById('output');
  // later this will get the actual OCR translation
  output.textContent = 'Waiting for selection...';
});
